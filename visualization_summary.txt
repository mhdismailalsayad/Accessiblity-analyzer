Issues per tool and page:
https://www.deutschebahn.com/: pa11y=2, axe=7, lighthouse=9, all=17

Most common issues:
3x Heading levels should only increase by one
3x Fix any of the following:
  Heading order invalid
2x Fix any of the following:
  ARIA role group is not allowed for given element
2x Fix any of the following:
  Text inside the element is not included in the accessible name
1x This form does not contain a submit button, which creates issues for those who cannot submit the form using the keyboard. Submit buttons are INPUT elements with type attribute "submit" or "image", or BUTTON elements with type "submit" or omitted/invalid.
1x This element's role is "presentation" but contains child elements with semantic meaning.
1x ARIA role should be appropriate for the element
1x ARIA attributes must conform to valid values
1x Landmarks should have a unique role or role/label/title (i.e. accessible name) combination
1x All page content should be contained by landmarks
